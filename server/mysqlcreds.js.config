const creds = {
    host     : 'host',
    user     : 'user',
    password : 'password',
    database : 'database', 
    port: port
  };

module.exports = creds; //when something requires this file, we'll give it creds
